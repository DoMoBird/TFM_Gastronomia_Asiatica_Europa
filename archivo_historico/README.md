# Archivo histórico — no usar para la entrega vigente

Esta carpeta conserva los modelos, métricas, notebooks, anexos, memoria y planificación anteriores a la revisión académica. Sus resultados no representan el experimento vigente y no deben mezclarse con él.

La versión vigente está en [revision_academica](../revision_academica/README.md). El modelo antiguo XGBoost se conserva solo como antecedente; los dos modelos reentrenados están en `../revision_academica/resultados/`.

Los notebooks históricos conservan sus rutas originales y no constituyen un flujo ejecutable desde esta carpeta. Para reproducir exactamente la estructura anterior, utilizar una copia separada del commit `0655656`; los CSV excluidos de Git deben obtenerse por separado. El notebook de carga que necesita la revisión permanece en `../notebooks/01_carga_exploracion.ipynb`.

Las fechas de este plan son históricas. Las conclusiones antiguas sobre inversión y las métricas anteriores están sustituidas por la revisión.
